import { EntityState } from '@ngrx/entity';
import { PortLabel } from '../models';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface PortLabelsState extends EntityState<PortLabel> {}
