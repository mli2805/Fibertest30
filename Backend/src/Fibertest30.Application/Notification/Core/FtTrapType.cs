namespace Fibertest30.Application;


// should be the same as in MIB file

public enum FtTrapType
{
    OpticalEvent = 100,
    RtuNetworkEvent = 200,
    BopNetworkEvent = 300,
    RtuStatusEvent = 400,
    TestTrap = 777,

}